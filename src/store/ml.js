import {defineStore} from "pinia";

const BASE_URL = "";

export const useMlStore= defineStore({
    id: "ml-store",
    state: () => ({

    }),
    actions: {
        predictValues(data){

        }
    }
})